_______________________________________________________________________________________
БЛОЧНОЕ РАСПОЛОЖЕНИЕ И СВОЙСТВО display

HTML:
<span>First</span>
<span>Second</span>
<span>Third</span>
CSS:
span {
 display: block;
}

строковые теги span будут отображаться и распологаться как блоки потому как свойство
display указано как block(теги будут распологаться каждый с новой строки)

p {
 display: inline; - все параграфы будут распологаться в одну линию отделяясь пробелами
}

значения свойства display:
block - каждый элемент с новой строки то есть отделяется с помощью переноса строки
contents - отображается лишь контент элемента к тому же бордюры проподают
стается один лишь контент элементы распологаются в одну линию через пробелы
flex - такие же свойства как и у block элементы распологаются с новой строки 
flow-root - такие же свойства как и у block
grid - такие же свойства как и у block
inherit - наследует у родителя свойство 
inline - в одну линию с бордюрами через пробелы
inline-block - в одну линию с небольшим отступом сверху
inline-flex - так же как и inline-block
inline-grid - так же как и inline-block
inline-table - так же как и inline-block но с небольшими отступами
list-item - каждый элемент является пунктом списком с маркером и распологаються каждый
с новой строки
none - удаляет полностью из окна
run-in - в одну строку
subgrid - в одну строку
table - каждый элемент с новой строки и без пустого места в ячейке место занимает лишь
контент
table-caption - так же как и table только слишком сильно урезается пустое место 
table-cell - так же как и table
table-column - не отображается
table-column-group - не отображается
table-footer-group - без бордюров с новой строки 
table-row - так же как и table-footer-group
table-row-group - так же как и table-footer-group
table-header-group - так же как и table-footer-group
_______________________________________________________________________________________
ВИДИМОСТЬ ЭЛЕМЕНТА ИЛИ ЕГО СКРТЫТИЕ
visibility: hidden
hidden - спрятать (после этого значения элемента не видно но место которое предпологается под элемент остается 
visible - показать
_______________________________________________________________________________________
СТАТИЧЕСКОЕ ПОЗИЦИОНИРОВАНИЕ (ПО УМАЛЧАНИЮ) static
Свойства top, bottom, left, и right игнорируются в нормальном позиционировании(static)
при значении static то есть при значении по умалчанию свойства position позиционирование 
не работает 

position: static;
_______________________________________________________________________________________
ФИКСИРОВАННОЕ ПОЗИЦИОНИРОВАНИЕ 

Элемент с фиксированной позицией размещается относительно окна браузера, и не будет двигаться, даже при прокрутке окна браузера.
Позиция может быть определена с помощью свойств top, right, bottom, и left.

Элементы с фиксированной позицией убираются из нормального потока. Документ и другие элементы ведут себя так, будто элемент с фиксированной позицией не существует.
Элементы с фиксированной позицией могут перекрывать другие элементы.

position: fixed;
_______________________________________________________________________________________
ОТНОСИТЕЛЬНО ПОЗИЦИОНИРОВАНИЕ 

Положение элемента задается относительно его исходного места. 
Свойства top, right, bottom, и left могут использоваться для определения сдвига элемента.

position: relative; 

Это значение не может быть использовано для ячеек таблиц, колонок, групп колонок, строк, групп строк, или заголовков.
_______________________________________________________________________________________
Абсолютное Позиционирование

Элемент абсолютного позиционирования расположен относительно первого родительского элемента, который имеет любое, но не нормальное, положение. Если таковой элемент не найден, то содержащий блок это <html>.

Абсолютно позиционированные элементы убраны из нормального потока. Документ и другие элементы ведут себя так, будто абсолютно позиционированные элементы не существуют.

position: absolute;

Абсолютно позиционированные элементы могут перекрывать другие элементы._______________________________________________________________________________________
Обтекание

Со свойством CSS float, элемент может быть выравнен по левому или правому краям, позволяя всем остальным элементам обтекать его вокруг.
Float часто используется с изображениями, но оно также полезно при работе с разметкой.
Значениями свойства float являются left, right, и none. 
Значения left и right свойства float выравнивают элементы в соответствующих направлениях (по левому и правому краям). none (по умолчанию) гарантирует, что элемент не будет обтекать. 

float: right;
_______________________________________________________________________________________
Использование свойства clear

Используйте значения right, left, и both для определения сторон элемента, где обтикание 
не будет действовать
right - запрещат обтекаие с правой стороны
left - запрещат обтекаие с левой стороны
both - запрещает обтекание со всех сторон
none - позволяет обтекание с двух сторон 
_______________________________________________________________________________________
Значения Свойства overflow

Существует четыре значения для свойства property: visible (значение по умолчанию), scroll, hidden, и auto. 

Значение scroll всегда добавляет полосу прокрутки.

overflow: scroll;

scroll - принудительно добавляются горизонтальная и вертикальная полосы прокрутки
visible - при нехватке места содержимое просто выходит за границы блока
hidden - при нехватке места содержимое скрывается
auto - при нехватке места появляються горизонтальная и вертикальная полосы прокрутки
_______________________________________________________________________________________
Свойство z-index

Когда элементы расположены вне нормального потока, они могут накладываться на другие элементы.
Свойство z-index определяет порядок накладывания элементов (какой из элементов будет расположен впереди, или сзади по отношению к другим).

CSS:

.blue {
   z-index: 3; 
   position: relative;
}
.red {
   z-index: 2;
   position: relative;
}
блок blue будет перекрывать блок red потому как значение свойства z-index больше
3 > 2 

Свойство z-index работает только с позиционированными элементами (position:absolute, position:relative, или position:fixed)._______________________________________________________________________________________